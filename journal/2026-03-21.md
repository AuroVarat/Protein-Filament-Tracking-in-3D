# 2026-03-21

Downloaded and extracted 3D TIFF files from Google Drive into `tiffs3d/`.

Files involved:
- `tiffs3d/` (107 TIFF files)


Created visualization script `scripts/tiff3d_to_3view_mp4.py` and generated 3-column orthogonal view MP4 in `tiffs3d_mp4/`.


Replaced orthogonal views with 5-Z-plane side-by-side videos in `tiffs3d_mp4/` using `scripts/tiff3d_to_5z_mp4.py`.


Replaced channel 0 videos with channel 1 (second channel) videos in `tiffs3d_mp4/`.


Applied per-slice (per-Z-plane) normalization to all videos for channel 1.
