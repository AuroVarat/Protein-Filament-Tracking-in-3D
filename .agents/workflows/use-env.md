---
description: Conda Environment Usage
---

Always use the local conda environment located in `./env` when working in this project.
It contains the necessary dependencies.

To run scripts, you can use the executable directly from the environment:
`./env/bin/python <script_name.py>`

// turbo-all
